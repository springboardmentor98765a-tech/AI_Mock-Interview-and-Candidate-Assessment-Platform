# OS Generated Files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Environment / Secret Files
# Covers variants too (.env.local, .env.bak-*, .env.production) — an exact
# ".env" rule alone would let those through.
.env
.env.*
*.env.bak*
!.env.example

# Frontend
frontend/node_modules/
frontend/dist/
frontend/dist-ssr/
frontend/.env

# Backend
backend/.venv/
backend/venv/
backend/ENV/
backend/env/
backend/__pycache__/
backend/*.py[cod]
backend/*.db
backend/*.sqlite3
backend/.env
# Uploaded candidate résumés — never commit these.
backend/uploads/

# IDE / Editors
.vscode/
.idea/
*.swp

# Local agent/tooling settings — machine-specific, not part of the project.
.claude/
